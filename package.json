{
  "name": "encorehub",
  "private": true,
  "version": "0.1.0",
  "description": "AI Chat Client - multi-provider, cross-platform",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "setup": "pnpm --dir frontend install --frozen-lockfile && go -C gateway mod download && cargo fetch --manifest-path engine/Cargo.toml",
    "setup:all": "pnpm run setup && uv sync --directory data-services --frozen --group dev",
    "dev": "pnpm run prepare:sidecar && pnpm --dir frontend tauri dev",
    "dev:frontend": "pnpm --dir frontend dev",
    "dev:gateway": "go -C gateway run ./cmd/gateway",
    "dev:engine": "cargo run --manifest-path engine/Cargo.toml --features standalone --bin encorehub-engine",
    "dev:data": "uv run --directory data-services uvicorn src.main:app --reload",
    "prepare:sidecar": "node scripts/prepare-gateway-sidecar.mjs",
    "check": "pnpm run test:contracts && pnpm run check:engine && pnpm run check:gateway && pnpm run check:frontend && pnpm run check:data",
    "check:engine": "cargo check --manifest-path engine/Cargo.toml --workspace",
    "check:gateway": "go -C gateway vet ./...",
    "check:frontend": "pnpm --dir frontend check",
    "check:data": "uv run --directory data-services mypy src/ tests/",
    "build": "pnpm run build:engine && pnpm run build:gateway && pnpm run build:frontend",
    "build:engine": "cargo build --manifest-path engine/Cargo.toml --release --features standalone --bin encorehub-engine",
    "build:gateway": "go -C gateway build -trimpath -o bin/encorehub-gateway ./cmd/gateway",
    "build:frontend": "pnpm --dir frontend build",
    "build:desktop": "pnpm run prepare:sidecar && pnpm --dir frontend tauri build",
    "test": "pnpm run test:contracts && pnpm run test:engine && pnpm run test:gateway && pnpm run test:data && pnpm run test:frontend",
    "test:contracts": "node --test scripts/workspace-contract.test.mjs scripts/docs-contract.test.mjs",
    "test:docs": "node --test scripts/docs-contract.test.mjs",
    "test:engine": "cargo test --manifest-path engine/Cargo.toml --workspace && cargo test --manifest-path engine/Cargo.toml -p encorehub-engine --all-targets --features standalone",
    "test:gateway": "go -C gateway test ./...",
    "test:frontend": "pnpm --dir frontend test --run && pnpm --dir frontend test:bundle",
    "test:data": "uv run --directory data-services pytest",
    "lint": "pnpm run test:contracts && pnpm run lint:engine && pnpm run lint:gateway && pnpm run lint:data && pnpm run lint:frontend",
    "lint:engine": "cargo fmt --manifest-path engine/Cargo.toml --all -- --check && cargo clippy --manifest-path engine/Cargo.toml --workspace --all-targets -- -D warnings && cargo clippy --manifest-path engine/Cargo.toml -p encorehub-engine --all-targets --features standalone -- -D warnings",
    "lint:gateway": "go -C gateway vet ./...",
    "lint:frontend": "pnpm --dir frontend lint",
    "lint:data": "uv run --directory data-services ruff check src/ tests/ && uv run --directory data-services mypy src/ tests/",
    "format": "pnpm run format:engine && pnpm run format:gateway && pnpm run format:frontend",
    "format:engine": "cargo fmt --manifest-path engine/Cargo.toml --all",
    "format:gateway": "go -C gateway fmt ./...",
    "format:frontend": "pnpm --dir frontend lint:fix",
    "docker:build": "docker compose build --no-cache",
    "docker:build:data": "docker compose --profile data build --no-cache data-services",
    "docker:up": "docker compose up -d",
    "docker:up:data": "docker compose --profile data up -d data-services",
    "docker:ps": "docker compose --profile data ps",
    "docker:down": "docker compose --profile data down",
    "proto:gen": "buf generate"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
